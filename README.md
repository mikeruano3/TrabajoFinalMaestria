# Installing Docker Compose

Run the following
```
sudo chmod +x installCompose.sh
./installCompose.sh
```

# Running the project

Run the following
```
cd infrastructure
docker-compose build
docker-compose up -d
```